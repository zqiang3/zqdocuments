HTML文档中所有的内容都是节点

整个文档是一个文档节点
每个HTML元素是元素节点
HTML元素内的文本是文本节点
每个HTML属性是属性节点
注释是注释节点


整个HTML文档是一个节点树
通过HTML DOM， 所有节点均可通过Javascript进行访问


可通过节点的innerHTML属性来访问文本节点的值

所有HTML元素被定义为对象，而编程接口则是对象和对象属性

innerHTML属性设置或返回开始和结束标签之间的HTML